# 1. Clone the repository (if not already cloned)
git clone https://github.com/your-username/your-repo.git
cd your-repo

# 2. Delete the file or folder
rm path/to/file.txt              # Delete a file
rm -r path/to/folder/            # Delete a folder

# 3. Stage and commit the deletion
git add .
git commit -m "Deleted file.txt and folder"

# 4. Push the changes to GitHub
git push origin main  # or your active branch

